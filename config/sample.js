export const config = {

    "node-port": 9898,
    
    "mysql": [
    
        {
            name:'name',
            host: 'host',
            user: 'user',
            password: 'pasword',
            port: 'port',
            database: 'database name',
            connectionLimit: 'limit',
        }
    
    ]
}